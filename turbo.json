{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "lint": {},
        "format": {},
        "test": {
            "inputs": [
                "src/**/*.mjs"
            ]
        },
        "deploy": {
            "dependsOn": [
                "format",
                "test",
                "lint"
            ]
        }
    }
}
